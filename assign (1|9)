#include<stdio.h>
int factorial(int x);
void main()
{
 int a,b;
 clrscr();
 printf("enter no");
 scanf("%d",&a);
 b=factorial(a);
 printf("%d",&b);
 getch();
 }
 int factorial(int x)
 {
   int r;
   if(x==1)
   return 1;
   else
   r=x*factorial(x-1)
   return r;
  }
 output:
 enter 6
 120 
 
